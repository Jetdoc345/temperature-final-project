console.log("function");

// convert celcius to farenheit project //

        function converttemperature()
        
        {
       
        let Fahrenheit = (deg F);
        let Celcius = (deg C);
        for deg F = (deg c * const1)
        for deg c = (deg f - const2)

        let const1 = (" 9/5) + 32  ") for deg F
        let const2 = (" 32) * 5/9  ") for deg c
        
        }


